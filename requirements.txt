# Five-Word Sentence Reconstruction Transformer
# Complete pinned Python environment used by the finished project.
#
# Required interpreter: CPython 3.11 (tested with Python 3.11.9)
# Tested platform: Windows, CPU-only PyTorch
# Tested installer: pip 26.1.2
#
# Create and activate a virtual environment before installation:
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# argparse, collections, datetime, hashlib, importlib.metadata, json, math,
# pathlib, platform, random, shutil, tempfile, typing, and unittest are part of
# Python 3.11's standard library and must not be installed separately.

# ---------------------------------------------------------------------------
# Direct project dependencies
# ---------------------------------------------------------------------------

# Transformer model, tensors, optimisation, DataLoader, and SummaryWriter.
torch==2.0.1

# Numerical arrays and deterministic random seeding used by training.
numpy==1.24.4

# WordLevel tokenizer, whitespace pre-tokenizer, and tokenizer persistence.
tokenizers==0.13.3

# Training metric event files and the TensorBoard web dashboard.
tensorboard==2.13.0

# Per-epoch training progress bars.
tqdm==4.66.1

# Educational model-inspection notebook kernel and visualizations.
ipykernel==6.29.5
matplotlib==3.7.5
nbclient==0.10.2
nbformat==5.10.4

# ---------------------------------------------------------------------------
# Fully pinned transitive dependency closure
#
# pip would normally install these automatically. They are listed explicitly
# so a fresh Python 3.11 environment reproduces the exact dependency versions
# that successfully ran data loading, all 31 tests, 100-epoch training,
# validation, evaluation, TensorBoard logging, inference, and the parameter
# visualization notebook in this project.
# ---------------------------------------------------------------------------

# PyTorch dependency chain.
filelock==3.32.3
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
sympy==1.14.0
typing-extensions==4.16.0

# tqdm dependency on Windows.
colorama==0.4.6

# TensorBoard core dependency chain.
absl-py==2.5.0
grpcio==1.83.0
Markdown==3.10.3
packaging==26.2
protobuf==7.35.1
setuptools==65.5.0
tensorboard-data-server==0.7.2
Werkzeug==3.1.8
wheel==0.48.0

# TensorBoard authentication and HTTP dependency chain.
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
cryptography==50.0.0
google-auth==2.56.3
google-auth-oauthlib==1.0.0
idna==3.18
oauthlib==3.3.1
pyasn1==0.6.4
pyasn1-modules==0.4.2
pycparser==3.0
requests==2.34.2
requests-oauthlib==2.0.0
urllib3==2.7.0

# Jupyter kernel and Matplotlib dependency chains used by the educational
# parameter/attention visualization notebook.
asttokens==3.0.2
attrs==26.1.0
comm==0.2.3
contourpy==1.1.1
cycler==0.12.1
debugpy==1.8.21
executing==2.2.1
fastjsonschema==2.22.2
fonttools==4.63.0
ipython==9.16.1
ipython-pygments-lexers==1.1.1
jedi==0.20.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
jupyter-client==8.9.1
jupyter-core==5.9.1
kiwisolver==1.5.0
matplotlib-inline==0.2.2
nest-asyncio==1.6.0
parso==0.8.7
pillow==12.3.0
platformdirs==4.11.3
prompt-toolkit==3.0.53
psutil==7.2.2
pure-eval==0.2.3
Pygments==2.20.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pyzmq==27.1.0
referencing==0.37.0
rpds-py==2026.6.3
six==1.17.0
stack-data==0.6.3
tornado==6.5.8
traitlets==5.16.1
wcwidth==0.8.2
